
        {/* Menu Items */}
        {/* <div className="flex justify-center items-center gap-4 flex-wrap">
          {menuList.map((curElem, index) => (
            <button
              key={index}
              className="px-6 py-2 text-black bg-gray-200 hover:bg-purple-600 hover:text-white rounded-lg transition duration-300 ease-in-out transform hover:scale-105"
              onClick={() => filterItem(curElem)}
            >
              {curElem}
            </button>
          ))}
        </div> */}